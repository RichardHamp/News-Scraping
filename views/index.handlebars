  
<div>
    <h1 class="center-align headline">Latest Headlines from "The Boulder Daily Camera"</h1>
    <div class="row">
        <div class="col s3"></div>
        <h5 class="center-align col s6 byline">Click 'Scrape' to get the latest articles</h5>
    </div>
</div>
<div class="row">
    <div class="col s2"></div>
    <div class="col s8 left-align">
        {{#each article}}
        <div class="card-panel">
            <h4>{{this.title}}</h4>
            <a href="/readArticle/{{this._id}}" class="waves-effect waves-light btn" id="btnSignUp">Read Article</a>

        </div>

        {{/each}}
    </div>
</div>